# Legacy `cpp_port` directory

Ported source code has been moved to top-level siblings:

- C++ core/C API/desktop/tests: `phic_port/`
- Web shell (TypeScript + Vite): `phic_web/`

This `cpp_port/` directory is kept only for existing local build/dependency artifacts.
